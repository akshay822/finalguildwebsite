<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>WoW Classic Guild</title>

    <link rel="stylesheet" type="text/css" href="/assets/stylesheets/a1.css">
    <link rel="icon" type="image/png" href="/assets/images/wowicon2.ico">

  </head>
  <body>
    <% include partials/header.ejs %>

    <% if(logged === true){ %>
      <% include partials/loggednav.ejs %>
    <% } else{ %>
      <% include partials/nav.ejs %>
    <% } %>

    <main>
      <section>
        <% if(logged === true){ %>
          <% include partials/loggedUpdateConnection.ejs %>
        <% } else{ %>
          <p> Must be logged in to update a connection. </p>
        <% } %>

        <% if(invalid === true){ %>
          <p>Update failed. Invalid input. Please try again.</p>
        <% } %>
      </section>
    </main>

    <% include partials/footer.ejs %>

  </body>
</html>
